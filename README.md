# bddkit-browser

A `bddkit` plugin that drives a real browser, headless or visible, in the same `.feature` file as the API and DB steps. Resource group: `browser`.

Work in progress; see `examples/README.md` once it lands.
